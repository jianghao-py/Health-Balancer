<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:tools="http://schemas.android.com/tools"
    package="com.example.comp3004project">

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/AppTheme">
        <activity android:name=".ui.home.WorkOutRecordActivity"></activity>
        <activity android:name=".ui.dashboard.WorkOutActivity" />
        <activity android:name=".ui.dashboard.FoodEventActivity" />
        <activity android:name=".ui.home.RecordActivity" />
        <activity android:name=".ui.notifications.SetGenderActivity" />
        <activity android:name=".ui.notifications.SetAgeActivity" />
        <activity android:name=".ui.notifications.SetWeightAndHeight" />
        <activity android:name=".LoginFunction.Login" />
        <activity android:name=".ui.dashboard.CreateEventActivity" />
        <activity android:name=".ui.dashboard.NewEventActivity" />
        <activity android:name=".ui.notifications.PersonalFileActivity" />
        <activity android:name=".ui.notifications.SetNameActivity" />
        <activity android:name=".LoginFunction.Resgister">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
        <receiver android:name=".ui.AlarmReceiver">
            <intent-filter>
                <action android:name="android.media.action.DISPLAY_NOTIFICATION" />

                <category android:name="android.intent.category.DEFAULT" />
            </intent-filter>
        </receiver>
        <activity
            android:name=".MainActivity"
            android:label="@string/app_name" />
    </application>


</manifest>